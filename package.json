{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "prepare": "husky",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run unit.test",
    "test:integration": "vitest run int.test",
    "test:api": "vitest run api.test",
    "test:coverage": "vitest run --coverage",
    "lint": "prettier --check .",
    "lint:eslint": "eslint .",
    "lint:fix": "prettier --write .",
    "lint:eslint": "eslint .",
    "lint:eslint:fix": "eslint . --fix",
    "format:check": "prettier --check . || (printf '\\n[Format check failed] One or more files are not formatted correctly.\\nRun: pnpm run lint:fix\\n\\n' >&2 && exit 1)",
    "seed": "pnpm --filter @workspace/scripts run seed-rbac",
    "seed:demo": "pnpm --filter @workspace/scripts run seed-demo",
    "seed:demo:reset": "pnpm --filter @workspace/scripts run seed-demo:reset",
    "check-map-cleanup": "pnpm --filter @workspace/scripts run check-map-cleanup",
    "ci": "pnpm run typecheck && pnpm run check-map-cleanup && pnpm --filter @workspace/scripts run test && pnpm --filter @workspace/scripts run test-map-cleanup && pnpm run test:unit && pnpm run format:check && pnpm run lint:eslint"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{js,mjs,cjs,jsx,json,css,md,yaml,yml}": [
      "prettier --write"
    ]
  },
  "private": true,
  "devDependencies": {
    "@types/supertest": "^7.2.0",
    "@typescript-eslint/eslint-plugin": "^8.59.2",
    "@typescript-eslint/parser": "^8.59.2",
    "@vitest/coverage-v8": "^4.1.2",
    "eslint": "^10.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.2",
    "prettier": "^3.8.1",
    "supertest": "^7.2.2",
    "typescript": "~5.9.2",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "docx": "^9.6.1"
  }
}

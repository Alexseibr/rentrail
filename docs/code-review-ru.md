# Code Review (русский)

Дата ревью: 11 мая 2026.

## 1) Что уже есть в проекте

Проект выглядит как зрелый TypeScript monorepo на `pnpm` с хорошей инженерной базой:

- Единый набор quality-gates на уровне root (`typecheck`, `lint`, `test`, `ci`).
- Явная архитектурная декомпозиция на `artifacts/*` (приложения) и `lib/*` (переиспользуемые библиотеки).
- Богатая доменная модель в БД (много таблиц, enum-ы, индексы, связи), реализованная через Drizzle.
- Четко описанные практики эксплуатации в документации (`docs/rollout`, `docs/playbooks`, `docs/release-readiness`).

## 2) Оценка качества кода

### Сильные стороны

1. **Инженерная дисциплина.**
   В `package.json` есть отдельные скрипты для проверки типов, тестов, линтинга и CI.

2. **Хорошая типизация в data layer.**
   В `lib/db/src/schema/*` таблицы описаны явно, плюс есть Zod-схемы для insert-моделей (`createInsertSchema`).

3. **Зрелая доменная модель.**
   По структуре видно, что это полноценная SaaS-платформа (RBAC, billing, moderation, telemetry), а не простой CRUD.

4. **Сильная эксплуатационная документация.**
   Есть rollout/playbook документы, что важно для поддержки и релизов.

### Текущие риски/проблемы

1. **Сломан базовый typecheck для библиотек без codegen-артефактов.**
   `pnpm run typecheck:libs` падает на `lib/api-client-react` и `lib/api-zod` (нет `./generated/*`).

2. **Неявный контракт bootstrap/codegen.**
   Для нового разработчика отсутствует "безошибочный путь" от clone до green-check.

3. **Риск регрессий на стыке API-контрактов и генерации клиентов.**
   При изменении OpenAPI возможна поломка потребителей, если codegen не встроен в обязательные шаги.

## 3) Проверка дизайна (UI/UX)

Проверил дизайн-структуру на уровне компонентов и навигации (без ручного визуального QA в браузере/эмуляторе):

### Что хорошо

1. **Структурированная навигация и role-based фильтрация.**
   В `AppLayout` есть группировка меню (main/tools, main/management) и фильтрация доступных пунктов по ролям.

2. **Переиспользуемый компонентный слой.**
   Есть унифицированные UI-компоненты (например, `Empty`, `PageBreadcrumb`), что обычно уменьшает визуальный дрейф.

3. **Поддержка локализации из коробки.**
   В layout есть переключение языка `ru/en`.

### Что улучшить по дизайну

1. **Фиксировать дизайн-стандарты как enforceable checks.**
   Рекомендую добавить visual regression (Playwright snapshots / Chromatic / Loki), иначе дизайн-регрессии ловятся только руками.

2. **Единый UX checklist для ключевых экранов.**
   Для страниц Fleet/Rentals/Service/Branches: пустые состояния, ошибки загрузки, длинные таблицы, mobile breakpoints.

3. **Accessibility budget.**
   Ввести обязательный проход `axe` для основных экранов и минимум WCAG AA для контрастов/фокуса/клавиатурной навигации.

## 4) Проверка E2E/интеграционных тестов

### Что фактически проверено

Запущен `pnpm run test:api` (API/E2E набор на базе Vitest). В текущем окружении тесты не смогли стартовать полноценно из-за отсутствия `DATABASE_URL`.

### Текущее состояние

- Упало **18 test suites**.
- Основная причина: `DATABASE_URL must be set`.
- Это ограничение окружения, а не обязательно дефект бизнес-логики тестов.

### Можно ли "проверить E2E всех функций"

Коротко: **да, но не в этом окружении без поднятой БД и bootstrap данных**.

Что нужно для полноценного прогона:

1. Поднять PostgreSQL и выставить `DATABASE_URL`.
2. Прогнать миграции/инициализацию данных (если требуется тестовым setup).
3. Выполнить:
   - `pnpm run test:api`
   - `pnpm run test:integration`
   - (опционально) web/mobile UI E2E через Playwright/Detox, если добавите такие сценарии.

## 5) Приоритетный план улучшений

### P0 (сразу)

1. Автоматизировать codegen перед `typecheck`/`build` (например, через `pretypecheck`).
2. Сделать обязательный bootstrap-сценарий в README/CONTRIBUTING.
3. Добавить reproducible local test stack (docker-compose для Postgres + seed).

### P1 (ближайший спринт)

4. Включить `codegen:check` в обязательный CI-путь до тестов.
5. Добавить visual regression для platform-admin.
6. Добавить smoke E2E user journeys (login → rental flow → payment flow).

### P2 (поддерживаемость)

7. Зафиксировать ADR по tenant isolation boundary.
8. Расширить quality-metrics: mutation testing на критичных доменных сценариях.

## 6) Итог

**Общее качество: выше среднего / ближе к высокому.**
Архитектура и инженерная культура уже хорошие. Главные точки роста — воспроизводимый bootstrap (codegen+db) и автоматизированная проверка UI/E2E, чтобы "проверка всех функций" была действительно стабильной и повторяемой.

## 7) Конкретные рекомендации (что делать прямо сейчас)

Ниже — практичный, короткий план без «воды».

### На этой неделе

1. **Починить воспроизводимость окружения:**
   - добавить `docker-compose` для Postgres,
   - добавить `pnpm run dev:bootstrap` (codegen + migrate + seed),
   - добавить `.env.example` с обязательными переменными.

2. **Зашить codegen в pipeline:**
   - `pretypecheck`/`prebuild` для генерации клиентов,
   - оставить `codegen:check` как блокер в CI.

3. **Сделать smoke-проход критичного флоу:**
   - auth → создание аренды → завершение аренды → платеж.

### В течение 2–3 недель

4. **Добавить визуальные регрессии для platform-admin:**
   - snapshot-тесты для ключевых страниц (Dashboard/Fleet/Rentals/Service).

5. **Собрать test-matrix по ролям и tenant isolation:**
   - owner/admin/mechanic/platform\* роли,
   - позитивные и негативные кейсы доступа.

6. **Усилить observability для тестов:**
   - структурированные логи падений,
   - сохранение SQL/HTTP trace в CI-артефакты.

### В течение 1–2 месяцев

7. **Формализовать архитектурные решения (ADR):**
   - tenant boundaries,
   - стратегия денежных расчетов (`numeric`, округления, валюты),
   - политика обновления `updatedAt` и аудита изменений.

8. **Повысить устойчивость релизов:**
   - mandatory release checklist,
   - canary rollout для рискованных изменений,
   - post-release health gates.

### Если выбрать только 3 приоритета

1. **Bootstrap + DB reproducibility.**
2. **Codegen как обязательный шаг перед typecheck/build.**
3. **Smoke E2E для денежного и rental флоу.**

Это даст максимальный эффект по качеству за минимальное время.

## 8) Что уже сделано в ветке (обновление на 11 мая 2026)

По факту в текущей ветке уже реализован большой кусок из P0/P1:

1. **Воспроизводимый dev bootstrap**
   - добавлены `dev:bootstrap`, `dev:verify`, `dev:test`;
   - добавлены сценарии проверки окружения (`dev:doctor`, `dev:doctor:json`).

2. **Инфраструктура локальной БД**
   - добавлен `docker-compose.dev.yml`;
   - добавлены команды `dev:db:up`, `dev:db:down`, `dev:db:logs`.

3. **Fail-fast для DB-зависимых тестов**
   - `test:api`, `test:integration`, `test:db-helpers` теперь проверяют `DATABASE_URL` заранее через обёртку.

4. **Codegen встроен в обязательные шаги**
   - добавлены `pretypecheck` и `prebuild`, которые запускают `codegen`.

5. **Небольшие UI/a11y-улучшения**
   - улучшена семантика кликабельного mobile-overlay в layout;
   - `EmptyDescription` переведен на семантический `<p>`.

Итого: базовый фундамент «поднять проект и проверить по шагам» уже есть.

## 9) Что дальше по плану (предлагаю следующий порядок)

### Шаг 1 (сразу, 1-2 дня): закрыть CI-дыры

1. Сделать `codegen:check` обязательным в CI до тестов.
2. Добавить отдельный CI job на `dev:doctor:json` (валидация preflight-логики).
3. Добавить fail-fast проверку, что generated-файлы не забыты в PR.

### Шаг 2 (ближайший спринт): покрыть критичный пользовательский путь

4. Добавить smoke E2E сценарий:
   - login,
   - создание аренды,
   - завершение аренды,
   - проверка платежа/статуса.

5. Зафиксировать минимальный test-data bootstrap для E2E (fixtures/seed).

### Шаг 3 (после smoke E2E): дизайн и UX-стабильность

6. Подключить visual regression для `platform-admin` (минимум ключевые страницы).
7. Добавить a11y smoke (axe) для sidebar, диалогов и основных форм.
8. Ввести короткий UI-чеклист для PR (empty/loading/error/mobile/focus).

## 10) Что делаем прямо следующим коммитом

Если идём строго «по плану», то **следующий практический коммит**:

- подключить `codegen:check` в обязательный CI пайплайн,
- и добавить короткий guard-job на generated-артефакты.

Это самый быстрый шаг, который уменьшит регрессии до начала расширения E2E.

## 11) Статус плана (трекер)

- ✅ **Сделано:** bootstrap/codegen hooks, DB compose, doctor/verify/test orchestration, fail-fast DB guards, базовые UI/a11y-фиксы.
- 🟡 **В работе дальше:** CI-guard generated (дожать до обязательного policy во всех ветках/пайплайнах), smoke E2E критичного денежного и rental flow.
- ⏭️ **После этого:** visual regression + a11y smoke для ключевых экранов.

Рекомендуемый порядок следующей реализации:

1. Включить обязательный прогон `pnpm run codegen:check` в каждом CI workflow (не только в локальном `ci.sh`).
2. Добавить `smoke`-проект для E2E/API с 1 критическим флоу и фикстурами.
3. Подключить минимальный visual regression для `platform-admin` (Dashboard/Fleet/Rentals).
